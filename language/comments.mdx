---
title: Comments
---

Tide currently supports single-line comments:

```tide
// this is a comment
relation double where
  double(x) = x * 2
```

(If you want block comments like `/* ... */`, that can be added later — alpha priorities are correctness + clarity first.)
